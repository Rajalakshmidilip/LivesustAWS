---
version: 0.2
phases:
  install:
    commands:
      - npm install
  pre_build:
    comands: Nothing to DO here
  build:
    commands:
      - echo 'date'
  post_build:
    commands:
      - echo build is completed